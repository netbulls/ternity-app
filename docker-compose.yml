services:
  postgres:
    image: postgres:17
    container_name: ternity-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: ternity
      POSTGRES_PASSWORD: ternity
      POSTGRES_DB: ternity
    volumes:
      - pgdata:/var/lib/postgresql/data

  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: ternity-gotenberg
    restart: unless-stopped
    ports:
      - '3030:3000'

volumes:
  pgdata:
