{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Pilot: mutation-test the one file in @ternity/shared with real branching logic. TZ=UTC is set by the test:mutation script so resolveDateRangePreset date assertions are deterministic.",
  "testRunner": "vitest",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "mutate": ["src/reports.ts"],
  "coverageAnalysis": "perTest",
  "reporters": ["clear-text", "progress"],
  "concurrency": 4,
  "timeoutMS": 20000
}
