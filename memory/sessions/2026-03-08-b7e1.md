# Session 2026-03-08-b7e1

## Context

Continuing the PDF measure-then-paginate migration. V1–V4 were done previously. This session completed V5, V6, V7 + cleanup.

## Completed

### V5 (dashboard-print)

- Div-based template, card layout
- Removed `collectUserRows`, `paginateRows`, `estimateLines` imports
- Built all data rows as `<div data-row>` elements in hidden measure container
- Added `[data-row] { overflow: hidden }` and `.user-detail-card { margin-bottom: 0 }`
- Nav bar, footer, gen-info card as `<template>` elements
- Pagination script: measures available height from card body marker to footer, distributes rows
- Widow divider fix: strips `.has-divider` from first row on each page
- Gen-info card appended to last page by script
- Static footer unicode fix: used `\u00b7` and `\u2013` directly in HTML context
- 10 → 9 pages, no overflow

### V6 (invoice-style)

- Table-based, unique dual-capacity layout (page 1 = invoice header + summary + partial table, cont = compact header)
- Removed `paginateRows`, `RowItem` imports
- Page 1 rendered statically with empty `<tbody id="p1-table">`; script fills it
- Script measures two different available heights: `page1AvailH` (from table to footer on page 1) and `contAvailH` (from page-content inner to footer on continuation pages)
- First batch of rows fills page 1 remaining space, rest goes to continuation pages with compact headers
- Appendix page remains static (placed after script-generated pages)
- Reduced `.page-content` bottom padding from 18mm to 10mm
- 6 → 8 pages (old 6 was undercounting — heuristic assumed 44 rows/page but some rows wrap to 2 lines, old code silently overflowed)

### V7 (cover-chapters)

- Most complex template: cover + TOC + exec summary + continuous data pages + closing block
- Removed `collectUserRows`, `paginateRows`, `fitRows`, `estimateLines` imports
- Cover page = static, TOC page = static with placeholder page numbers
- Exec summary page rendered statically with chapter heading + metrics + chart + user0 header + empty table
- All data rows (all users) rendered into measure container
- Script measures `execAvailH` (from table on page 3 to footer) and `contAvailH`
- Script fills page 3 table, builds continuation pages
- TOC page numbers updated by script: searches pages for `data-user` attribute matching each user name
- Closing block on last page via `<template id="tpl-closing">`
- Widow divider fix: strips `has-divider` from first row on each continuation page
- Removed dead `renderUserSection()` function
- Reduced `.page-content` bottom padding from 20mm to 10mm, removed `entries-table margin-bottom`
- 13 → 11 pages, no overflow

### Cleanup

- Removed all heuristic pagination helpers from `shared.ts`:
  - `estimateLines()`, `collectUserRows()`, `fitRows()`, `paginateRows()`
  - Types: `RowItem`, `DayGroup`, `Entry`, `PageChunk`
- No template imports these anymore — all 7 use measure-then-paginate

## Final Page Count Summary

| Template             | Old (heuristic) | New (measured) |
| -------------------- | --------------- | -------------- |
| V1 Classic Corporate | 10              | 9              |
| V2 Dark Executive    | 10              | 9              |
| V3 Minimal Swiss     | 9               | 8              |
| V4 Magazine Spread   | 12              | 9              |
| V5 Dashboard Print   | 10              | 9              |
| V6 Invoice Style     | 6               | 8              |
| V7 Cover Chapters    | 13              | 11             |

Note: V6 increased from 6→8 because the old heuristic was dishonest (assumed 44 rows/page, some rows wrap, Chromium silently overflowed). The new measured approach is truthful.
