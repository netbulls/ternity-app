# Session 2026-03-06-9b7a

## Context

User asked "What did we do so far?" — continuation after yesterday's massive session. Reviewed all 4 session logs from 2026-03-05 (2b8c, 2148, e8a1, c7d3) plus compacted context.

## Status

Last commit: `2e2f0ee feat: leave management, timezone fixes, UI overhaul` — deployed to dev, verified clean. Next steps from previous session: manual dev verification, then prod deployment.

## Replicated team assignments to dev

User reported team assignments were missing on dev. Ran UPDATE statements matching local assignments (25 users across 6 teams) to dev by display_name → project name mapping.

Mapping: DevOps (local) = "Devops - duty time" (dev), HR/Legal500/Mercaso/Shaype/Yosensi matched by name.

Result: 26 rows updated (one extra due to duplicate "Jakub Sobecki" on dev, both assigned to Mercaso). All 6 teams populated.

## "View Entries" from Projects admin

Added ability to navigate from a project's action menu to the Entries page with that project pre-filtered.

**Changes:**

- `entries.tsx`: `filterProjectId` now reads `?projectId=xxx` from URL params on mount (cleared after reading)
- `project-columns.tsx`: Added "View Entries" action (List icon) to project row dropdown, new `onViewEntries` callback option
- `projects.tsx`: Added `useNavigate`, `handleViewEntries` callback navigating to `/entries?projectId={id}`, wired into column definition

## Year view mode for entries

Added a "Year" view mode to the entries page, only available when a project filter is active. Prevents unbounded queries while allowing users to see all entries for a project within a year.

**Changes:**

- `format.ts`: Added `getYearStart()`, `getYearEnd()`, `shiftYears()` helpers. Updated `formatDateRange()` to show just "2026" for full-year ranges.
- `entries.tsx`:
  - ViewMode type extended with `'year'`
  - Auto-switches to year view when arriving with `?projectId` URL param
  - Year toggle button only shown when `filterProjectId` is set
  - Nav arrows shift by year in year mode
  - `handleProjectFilter` callback: clearing project filter snaps back from year to week view
  - "Clear filters" also triggers snap-back via `handleProjectFilter(null)`

## Admin user filter on Entries page

Added ability for admins to view any user's entries from the Entries page, without using the full impersonation system.

**Changes:**

- `entries.ts` (API): GET `/api/entries` accepts `?userId=xxx` (admin-only, 403 for non-admins)
- `use-entries.ts`: `useEntries()` accepts optional `userId` param, passes to API, included in cache key
- `user-selector.tsx` (new): Popover dropdown matching ProjectSelector style — search, avatars, alphabetical list of active users, "All people" clear option
- `entries.tsx`: Added `filterUserId` state, `UserSelector` shown for admins in filter bar (before Project filter), included in `hasFilters` and "Clear filters" logic

### Fix: "All people" now actually shows all entries

- UserSelector uses three-value semantics: `null` (no filter/my entries), `'all'` (everyone), or UUID (specific person)
- API `entries.ts`: `?userId=all` skips the user WHERE clause entirely
- Trigger label shows "Person" (no filter), "All people", or the selected user's avatar+name

### User info on entry rows

When a person filter is active (all people or specific person), entry rows show the user's avatar + name. Hidden when no person filter is set.

**Changes:**

- `time-entries.ts` (shared): Added optional `userName` and `userAvatarUrl` to EntrySchema
- `entries.ts` (API): Batch-loads user info when entries span multiple users. Skips for single-user queries.
- `entry-row.tsx`: New `showUser` prop — renders UserAvatar + name (fixed 130px col) before description
- `day-group.tsx`: Passes `showUser` through to EntryRow
- `entries.tsx`: Passes `showUser={!!filterUserId}` to DayGroup

## Deployed to dev + prod

Committed as `ecad4c1 feat: admin entries view — project drill-down, year view, person filter with user info on rows`. No migrations needed — code-only changes. Both environments verified clean (4/4 containers, API health, 9/9 SPA routes, auth gate, zero errors).
