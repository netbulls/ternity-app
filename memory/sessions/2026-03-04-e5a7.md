# Session 2026-03-04-e5a7

## Focus: Leave Management Research & Planning

### Context

User wants to focus on leave management. Starting with a full research pass to understand what exists, what's been discussed, and what needs to be built.

### Research Findings

#### What's Already Built

- **DB schema**: `leave_types`, `leave_allowances`, `leave_requests` tables all migrated and live
- **Timetastic sync**: Full pipeline operational — 23 leave types, 4,163 absences synced from Timetastic
- **Notification infrastructure**: Schemas + email/SMS templates for `leaveRequestUpdate` and `teamLeave`
- **Seed data**: Holiday (26 days/year) + Sick Leave (14 days/year)
- **UI placeholders**: Leave page + Calendar page in sidebar nav (both "Coming in Phase X")
- **Presence board**: "On Leave" as first-class presence status in prototypes

#### What's NOT Built

- API routes (CRUD for leave requests, approvals, allowance queries)
- Frontend leave request UI
- Frontend approval workflow
- Frontend leave calendar/wallchart
- Leave -> Presence integration (auto-set "On Leave")

#### Key Decisions from Past Sessions

- Build order: Team Board -> **Leave management** -> Presence marking -> Reconciliation
- Leave management described as "small, basic request+approval"
- Approved leave auto-sets presence to "On Leave"
- 4-hour rule: away 4+ hours -> treated as leave request
- Leave requests routed to designated project approver (manager role)

### Key Decision: Two Workforce Types, Contractor-First

#### The Legal Problem

In Poland, if contractors (B2B) go through a formal leave **approval** process, ZUS (social insurance authority) can treat this as evidence of an employment relationship — major legal risk. So contractors must NOT have an approval flow.

#### Decided Approach

1. **User profile gets `employment_type`** field — `contractor` vs `employee`
2. **Build contractor flow first** — simple "I'm booking time off" with no approval needed
3. **Add employee flow later** — formal request + manager approval (legally required for umowa o prace)
4. **For now, treat everyone as contractors** — even if someone is marked employee, approval flow doesn't exist yet

#### Status Model

- New status: **`autoconfirmed`** — used when contractor self-books time off (name makes it clear no human reviewed it)
- Existing `approved` status reserved for future employee flow (manager explicitly approves)
- DB enum `leave_status` needs to be extended: `pending`, `approved`, `rejected`, `cancelled` + **`autoconfirmed`**
- Contractor books time off -> immediately `autoconfirmed`
- Employee books time off -> `pending` -> manager approves -> `approved` (future)

### Booking Duration & Validation Decisions

1. **Partial-day bookings**: 1h increments up to 4h. Single date only. Aligns with presence 4-hour rule.
2. **Full-day bookings**: 4h+ = full day. Can span date ranges. Weekends + Polish public holidays auto-excluded from count.
3. **Overlap rules**: Full-day booking blocks the whole date. Partial-day bookings can stack on the same date up to 4h total. If a new booking would push past 4h, system blocks it with explanation — user must cancel existing partials and book a full day instead. No auto-conversion.
4. **Public holidays**: Hardcode Polish holidays for 2025–2027 initially. Extend later.

### Leave Wallchart Explorations Created

5 exploration pages built and registered in router. All type-check clean.

| Route           | Approach                      | Key differentiator                                                                          |
| --------------- | ----------------------------- | ------------------------------------------------------------------------------------------- |
| `/dev/leave-v1` | Dense grid (Timetastic-style) | People rows x day columns, color-coded cells, dashed borders for partials                   |
| `/dev/leave-v2` | Gantt bar wallchart           | Horizontal bars spanning leave dates, summary stats, visual duration                        |
| `/dev/leave-v3` | Card-per-person               | Each person gets a card with mini month-bar + booking details. Feed-style.                  |
| `/dev/leave-v4` | Hybrid grid + hover detail    | Compact grid with connected range fills, week separators, people-out footer, hover tooltips |
| `/dev/leave-v5` | Split view                    | My Leave sidebar (bookings, stats, CTA) + wallchart grid side-by-side. No tabs.             |

All explorations include:

- Full page shell (DevToolbar, providers, proper theme)
- Page header with "Leave" title + "Book time off" button
- Wallchart + My Leave tabs (or split view in V5)
- Month navigation, search, legend
- Mock data: 10 people, 11 bookings including partial-day bookings (2h, 3h, 4h)
- Polish public holidays, weekend highlighting, today marker
- Leave type color coding (Holiday=teal, Sick=orange, Personal=purple, Parental=pink, Unpaid=gray)
