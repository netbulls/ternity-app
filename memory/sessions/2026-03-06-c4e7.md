# Session 2026-03-06-c4e7

## Context

Continuation session. User asked to continue if there are next steps, or stop and ask for clarification. Reviewed previous session log (9b7a from today) and compacted context summary covering the full history of work.

## Status

Last deployment was clean on both dev and prod (commit `ecad4c1`). No uncommitted changes. Template standards check: all rules current.

## Leave type sync to dev and prod

User asked to make sure leave types on dev and prod match local. Found both remote environments identical to each other but differing from local in several ways.

**Changes applied (same on both dev and prod):**

1. Inserted "Holiday" (26 days/yr, deducted=true, active=true, visibility=all)
2. Inserted "Sick Leave" (14 days/yr, deducted=true, active=false, visibility=all)
3. Updated "Other" — active=false, visibility='contractor'
4. Renamed "Praca zdana OKAZJONALNA" → "Praca zdana okazjonalna"
5. Updated 16 (UoP) types — visibility from 'all' to 'employee'
6. Skipped creating empty "New Group" (no types assigned locally either)

All 26 leave types now match across all 3 environments. ~4k existing leave_requests on each env unaffected (FK by ID, not name).

## Restored archived explorations to dev lab

Old explorations were moved to `archive/brand-migration-2026-02-27/` in commit `9a33a97` but `lab-data.ts` still referenced the original `public/explorations/` paths. Restored all 115 missing HTML files (desktop: 12, login: 9, timer: 1, web: 93) back to `apps/web/public/explorations/`. All 130+ lab-data references now resolve. Total: 151 HTML exploration files on disk.

## Open items from previous sessions

- Manual verification of new features on dev/prod (person filter, year view, View Entries from projects)
- `stats.ts` queries use `timeEntries.createdAt` instead of segment timestamps (pre-existing)
- Polish holidays hardcoded through 2027 only (needs algorithm for 2028+)
- `orgMidnightToUTC` duplicated in 3 files (should extract to shared util)
- No leave allowance deduction on booking creation (feature not built yet)
- 30 dev prototype files in build output (gated, not accessible in prod)
