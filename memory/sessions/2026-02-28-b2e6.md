# Session 2026-02-28-b2e6

## Task: Rewrite notifications-v5-j9e7.html with full app chrome

**What was done:**

- Rewrote `apps/web/public/explorations/web/notifications-v5-j9e7.html` from a standalone panel layout to full app context
- Added app shell matching other explorations (e.g., working-hours-v5): sidebar (220px, logo, Tracking nav, spacer, user block "PR"), main area with Settings header + tabs
- Notifications tab is ACTIVE in the tab bar
- Content area max-width ~550px

**V5 content changes:**

- Removed the save button and save bar — this is now auto-save (no save button, the cleanest approach)
- Removed the panel-header with bell icon — settings header + tabs handle the context now
- Channels bar redesigned: rounded-lg muted/20 px-4 py-2.5, row with Mail icon + email + green dot | separator | Phone icon + number + green dot + "Test" link
- Notification list with column headers (EMAIL, SMS, AFTER), compact rows with 14px icons (not 32px icon boxes)
- Mini toggle switches (30x16px CSS-only)
- Groups separated by subtle 1px dividers (no group headers)
- Theme switcher at top, ternity-dark default, ../themes.css, Lucide CDN, Oxanium+Inter
