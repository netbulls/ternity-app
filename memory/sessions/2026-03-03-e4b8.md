# Session — 2026-03-03-e4b8

## Context

Continuation from 2026-03-02-59bd. That session built V2-V4 presence board explorations and wrote V5 but didn't register or verify it.

## Work Done

### V5 Registration & Build Fix

- V5 file (`dev-presence-v5.tsx`, 1148 lines) existed but wasn't registered
- Added lazy route in `router.tsx` at `/dev/presence-v5`
- Added entry in `lab-data.ts` in the presence group: "V5 — Status Roster"
- Fixed TypeScript errors: `PROJECT_COLORS` was `Record<string, string>` so bracket access returned `string | undefined`. Changed to `as const` with a `getColor()` helper that safely indexes with a fallback.
- Build passes clean

### V5 Iteration — Single-line rows + Away status

User feedback: rows shouldn't wrap, all statuses should be pills (including "Off Schedule").

**No-wrap fix:**

- Removed all sub-content below status badges (task text, "no timer running", "starts X:00", "holiday")
- Each row is now single-line: Person | Status pill | Timeline
- Added `whitespace-nowrap` to `PresenceBadge` so multi-word labels don't break
- Widened status column from 110px to 120px

**Away status (planned absence):**

- User wanted to differentiate idle (unexpected gap) from planned absence (known ahead of time)
- Added `'away'` to `PresenceStatus` with violet color scheme
- Added `AbsenceBlock` interface with `id`, `reason`, `startHour`, `endHour`
- Added optional `absences` array to `MockPerson`
- `STATUS_META` entry: violet dot, `bg-violet-400/10`, label "Away", sortOrder 2.5 (between idle and off-schedule)
- Absence blocks on timeline: striped violet pattern with border, shows reason text inside
- Added `CalendarOff` icon for the Away filter pill
- Updated legend with "Planned absence" swatch
- Mock data: Leo Tanner → away (half-day off 14-17), Tom Galloway → away (doctor appointment 13-15)

### V5 further iterations

- Removed idle hatching (gap = idle, background showing through is the signal)
- Renamed "Off Schedule" → "Off Hours"
- Off-hours people get no schedule background (entries float on bare timeline)
- Absence blocks moved to background layer (part of the plan, not foreground)
- Absence blocks simplified: solid violet tint, no stripes
- Legend simplified: only background concepts (Scheduled hours, Planned absence, On Leave, Now)
- Entry blocks show project name inside (truncate when narrow)
- Replaced custom HSL colors with real Ternity palette (10 hex colors from @ternity/shared)
- Text color auto-inverts using ITU-R BT.601 perceived luminance (hex-based)
- Separated color layer from text layer (color div has opacity, text stays fully opaque)
- Opacity bumped: 0.8 logged, 0.95 running/highlighted, 0.15 dimmed
- Removed brightness filter and text shadows
- Tooltip upgraded to match day-timeline pattern (project dot, name, description, time, duration)

### Moving to real implementation

- User asked to plan and implement the real Team Presence Board
- Wrote comprehensive plan at `memory/plans/team-presence-board.md`
- Captures ALL design decisions from V5 iterations
- Phase 1: Backend (team board endpoint using existing tables)
- Phase 2: Frontend (real page at /team with nav entry)
- Phase 3: SSE (deferred, polling first)
- No new DB migration needed — derives from existing users, working_schedules, time_entries, entry_segments, projects

## Next Steps

- Implement Phase 1: `GET /api/team/board` endpoint
- Implement Phase 2: Frontend page at `/team`
- Deploy
