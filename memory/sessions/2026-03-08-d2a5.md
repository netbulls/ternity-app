# Session 2026-03-08-d2a5

## Context

Continuation session. Building shared date/time picker components to replace native inputs across the app.

## Completed

### DateRangePicker component + Client Reports integration

- Ran `pnpm dlx shadcn@latest add calendar` in `apps/web` — scaffolded `calendar.tsx` (react-day-picker v9 + date-fns v4)
- Created `apps/web/src/components/ui/date-range-picker.tsx` — Popover + Calendar with `mode="range"`
- Replaced two native `<input type="date">` (From/To) in `client-reports.tsx` with single `<DateRangePicker>`
- Removed unused `handleDateFromChange` / `handleDateToChange` callbacks
- Trigger button shows "Mar 1, 2026 – Mar 31, 2026" format
- Selecting a range via calendar sets preset to "custom"
- Config: `weekStartsOn={1}` (Monday), `showWeekNumber`, `showOutsideDays={false}`, `numberOfMonths={2}`
- Build passes
